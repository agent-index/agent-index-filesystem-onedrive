{
  "name": "@agent-index/filesystem-onedrive",
  "version": "2.0.3",
  "description": "Agent Index Filesystem — Microsoft OneDrive/SharePoint adapter (on-demand executor)",
  "type": "module",
  "main": "src/exec.mjs",
  "bin": {
    "aifs-exec": "src/exec.mjs"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "build:exec": "esbuild src/exec.mjs --bundle --platform=node --target=node18 --format=esm --outfile=dist/aifs-exec.bundle.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "test": "node --test src/**/*.test.js"
  },
  "keywords": ["agent-index", "mcp", "onedrive", "sharepoint", "filesystem"],
  "author": "Agent Index",
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "@agent-index/filesystem": "file:../agent-index-filesystem",
    "undici": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
